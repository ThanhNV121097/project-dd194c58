# PostgreSQL connection URL injected by runtime or docker compose.
DATABASE_URL=postgres://app:app_secret@localhost:5432/app?sslmode=disable

# HTTP port for the backend service.
PORT=8080

# Optional fallback HTTP port when PORT is not set.
APP_PORT=8080
