# project-dd194c58
Project type: Full-stack web app — a frontend, a backend HTTP API and a database. Deliberately NOT a static page: the backend must be a real service and the entries must survive a restart.Customer request:A small guest book, the kind a shop puts by the door. Visitors leave their name and a short note; everyone can read what previous visitors wrote.
